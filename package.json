{
  "name": "browser-softphone-sipjs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "addon:sync": "node scripts/sync-addon-build.mjs",
    "build:addon": "tsc --noEmit && vite build && node scripts/sync-addon-build.mjs"
  },
  "dependencies": {
    "sip.js": "^0.21.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-mkcert": "^1.17.10"
  }
}
